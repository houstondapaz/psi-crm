services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: crm
      POSTGRES_DB: crm_psi
    ports:
      - "5433:5432"
    volumes:
      - postgres_data_17:/var/lib/postgresql/data

volumes:
  postgres_data_17:
